<div id="container" class="container-fluid" role="main">

  <div class="row">
    <%= erb :navbar %>

    <div class="row">
      <div class="col-md-4">
        <%= erb :order %>
      </div>

      <div class="col-md-8">

        <ol class="breadcrumb">
          <li><a href="#">Home</a></li>
          <li><a href="#">Library</a></li>
          <li class="active">Data</li>
        </ol>

        <div class="row">
          <table class="table">
            <tr>
              <td><%= erb :item, locals: {title: 'Голубцы'} %></td>
              <td><%= erb :category, locals: {title: 'Супы'} %></td>
              <td><%= erb :category, locals: {title: 'Салаты'} %></td>
            </tr>

            <tr>

              <td><%= erb :item, locals: {title: 'Пельмени'} %></td>
              <td><%= erb :item, locals: {title: 'Плов обычный'} %></td>
              <td><%= erb :item, locals: {title: 'Ля макароны ля c фаршем'} %></td>
            </tr>

            <tr>
              <td> <%= erb :category, locals: {title: 'Вторые блюда'} %></td>
            </tr>

          </table>
        </div>
      </div>
    </div>
  </div>
</div>
